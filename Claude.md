# runatic_seed_sim プロジェクト

ブラウザで動作するゲームの開発プロジェクト。

## ディレクトリ構成

```
runatic_seed_sim/
├── .gitignore
├── Claude.md
├── 参考資料/
│   └── 他ゲームの資料/       # 他ゲームの設計資料（参考用）
├── docs/
│   ├── 概念設計/             # ゲームコンセプト・世界観・コアメカニクス・ゲームループ
│   ├── 要件定義/             # 機能要件・非機能要件
│   ├── 実装設計/             # アーキテクチャ・UI/UX・ゲームフロー
│   ├── データモデル/         # エンティティ定義・ER図
│   ├── スプリント/           # スプリント計画・バックログ・振り返り
│   │   ├── README.md         # スプリント一覧・現在の状況
│   │   ├── バックログ.md     # 全スプリントのタスク一覧
│   │   └── sprint-XX/        # スプリントごとのディレクトリ
│   │       ├── 計画.md
│   │       └── 振り返り.md
│   ├── 調査資料/             # 設計判断のための調査・リサーチ結果
│   └── 開発日誌/             # 個人メモ・日誌（.gitignore で除外）
├── src/
│   ├── index.html            # エントリーポイント
│   ├── css/                  # スタイルシート
│   ├── js/
│   │   ├── main.js           # エントリーポイント
│   │   ├── core/             # コアロジック
│   │   ├── models/           # データモデル
│   │   ├── views/            # 画面表示
│   │   └── utils/            # ユーティリティ
│   └── assets/
│       ├── images/
│       └── sounds/
├── schema/                   # JSONスキーマ定義
├── data/
│   ├── test/                 # テスト用データ
│   └── initial/              # ゲーム初期データ
└── tests/
    ├── unit/
    └── e2e/
```

## ドキュメントのルール

- `docs/` 配下のサブディレクトリ名・ファイル名は日本語を使う
- 開発日誌は `docs/開発日誌/YYYY-MM-DD.md` の形式で記録する
- 開発日誌は `.gitignore` により git 管理対象外

## スプリント管理のルール

- 1スプリント＝1週間
- スプリント開始時に `docs/スプリント/sprint-XX/計画.md` を記入する
- 作業内容は毎日開発日誌に記録する
- スプリント終了時に `docs/スプリント/sprint-XX/振り返り.md` を記入する
- 完了したタスクは `docs/スプリント/バックログ.md` のチェックボックスを入れる
- `docs/スプリント/README.md` の状態欄を随時更新する

## Claudeの作業ルール

以下を必ず優先して行うこと。

1. **スプリントタスクの更新を最優先する**
   - タスクが完了したら即座に `docs/スプリント/sprint-XX/計画.md` のチェックボックスを更新する
   - あわせて `docs/スプリント/バックログ.md` のチェックボックスも更新する

2. **成果物はドキュメントとして必ず残す**
   - 調査・検討・決定したことは `docs/` 配下の該当ファイルに書き起こす
   - 調査を行った場合は `docs/調査資料/` にファイルを作成する

3. **ドキュメントにはversionと作成日を記載する**
   - データモデルなど更新が想定されるファイルは変更履歴を末尾に記載する

## 参考資料

- `参考資料/他ゲームの資料/` に他ゲームの設計資料を置く
- 現在: `lunatic_dawn_3_design.md.pdf`（Lunatic Dawn 3 の設計資料）
